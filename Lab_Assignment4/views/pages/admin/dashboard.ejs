<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: #f0f2f5;
            min-height: 100vh;
        }

        .dashboard-container {
            max-width: 1200px;
            margin: 2rem auto;
            padding: 2rem;
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        }

        .dashboard-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 3rem;
            padding-bottom: 1.5rem;
            border-bottom: 2px solid rgba(0, 0, 0, 0.05);
        }

        .dashboard-header h1 {
            font-size: 2.5rem;
            color: #2d3436;
            margin: 0;
            font-weight: 700;
            background: linear-gradient(45deg, #2d3436 30%, #636e72 90%);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .dashboard-stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            margin-top: 2rem;
        }

        .stat-card {
            background: white;
            padding: 2rem;
            border-radius: 15px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            border: 1px solid rgba(0, 0, 0, 0.05);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
        }

        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
        }

        .stat-card h3 {
            font-size: 1.5rem;
            color: #2d3436;
            margin-bottom: 1.5rem;
            font-weight: 600;
        }

        .btn {
            padding: 0.8rem 1.5rem;
            border-radius: 10px;
            font-weight: 500;
            text-decoration: none;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
            display: inline-block;
        }

        .btn-primary {
            background: linear-gradient(45deg, #0984e3 30%, #00cec9 90%);
            color: white;
            box-shadow: 0 4px 15px rgba(9, 132, 227, 0.3);
        }

        .btn-primary:hover {
            background: linear-gradient(45deg, #0770c5 30%, #00b5b0 90%);
            box-shadow: 0 6px 20px rgba(9, 132, 227, 0.4);
            transform: translateY(-2px);
        }

        .btn-danger {
            background: linear-gradient(45deg, #d63031 30%, #e17055 90%);
            color: white;
            box-shadow: 0 4px 15px rgba(214, 48, 49, 0.3);
        }

        .btn-danger:hover {
            background: linear-gradient(45deg, #c12929 30%, #d66a51 90%);
            box-shadow: 0 6px 20px rgba(214, 48, 49, 0.4);
            transform: translateY(-2px);
        }

        @media (max-width: 768px) {
            .dashboard-container {
                margin: 1rem;
                padding: 1.5rem;
            }
            
            .dashboard-header {
                flex-direction: column;
                gap: 1rem;
                text-align: center;
            }
            
            .dashboard-header h1 {
                font-size: 2rem;
            }
            
            .stat-card {
                padding: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <div class="dashboard-container">
        <div class="dashboard-header">
            <h1>Welcome, <%= username %></h1>
            <a href="/admin/logout" class="btn btn-danger">Logout</a>
        </div>

        <div class="dashboard-stats">
            <div class="stat-card">
                <h3>Products</h3>
                <a href="/admin/product" class="btn btn-primary">Manage Products</a>
            </div>
            
            <div class="stat-card">
                <h3>Categories</h3>
                <a href="/admin/category" class="btn btn-primary">Manage Categories</a>
            </div>
            <div class="stat-card">
                <h3>Orders</h3>
                <a href="/admin/order" class="btn btn-primary">Manage Orders</a>
            </div>

        </div>
    </div>
</body>
</html>